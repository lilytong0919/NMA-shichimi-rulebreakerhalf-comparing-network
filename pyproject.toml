[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "monkeypaw"
version = "0.1.0"
dependencies = [
    "numpy",
    "torch",
    "pandas",
    "pynapple",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "motornet"
]